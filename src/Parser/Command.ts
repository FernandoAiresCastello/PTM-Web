export enum Command {
    
    TEST = "TEST",
    DATA = "DATA",
    HALT = "HALT",
    RESET = "RESET",
    TITLE = "TITLE",
    SCREEN = "SCREEN",
    GOTO = "GOTO",
    CALL = "CALL",
    RET = "RET",
    VAR = "VAR",
}
